#include <stdio.h>

int main() {
    int num;
    scanf("%d",&a);

    if(num>="a" && num<="z"){
        printf("Lowercase");
    }else if (num>="A" && num<="Z"){
        printf("Uppercase");
    }else{
        printf("Not a alphabet";)
    }
    return 0;
}